apply plugin: "war"

bootWar {
    mainClassName = "org.travelers.posts.PostsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
